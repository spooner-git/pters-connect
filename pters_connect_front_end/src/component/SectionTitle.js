import React from 'react';

const SectionTitle = ({children, style}) => {
    return (
        <div style={style}>
            {children}
        </div>
    );
};

export default SectionTitle;